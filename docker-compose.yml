
version: '3.9'
services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    environment:
      - DATABASE_URL=postgresql://postgres:password@postgres:5432/saasdb
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
